✅ Melhorias Semânticas Ordenadas por Facilidade de Implementação
🟢 Mais Fáceis
Verificação da existência de funções chamadas
→ Validar em visitChamadaGenerica se a função foi declarada.
Basta consultar self.funcoes_declaradas.

Verificação da aridade (número de argumentos) nas chamadas de função
→ Contar argumentos passados e comparar com os declarados.
Exige manter uma tabela de funções com número de parâmetros.

Verificação de variáveis já declaradas no mesmo contexto
→ Já tens isto implementado, mas pode ser reforçado com mensagens detalhadas.

Verificação de índice de vetor ser do tipo inteiro
→ Verificar tipo da expressão de índice em visitAcessoVetor.
Necessita só de um sistema mínimo de inferência de tipos.

Verificação de return na função principal (main)
→ Proibir retorno de valor se main for void.

🟡 Intermédias
Verificação de tipos em atribuições
→ Ex: impedir int x = 3.14; sem cast.
Requer rastrear tipo de cada variável e expressão.

Verificação de tipos em expressões aritméticas e lógicas
→ Ex: impedir int + string ou double && int.
Requer árvore de tipos para operadores.

Verificação de tipo de retorno em funções não-void
→ Comparar tipo declarado da função com o da return.
Requer armazenar tipo de retorno da função atual durante visitFuncao.

Verificação de uso de funções built-in com tipos adequados
→ Ex: writes("msg") vs writes(3) → erro.
Só precisa de verificar argumento com getText().

Avisos sobre variáveis não inicializadas
→ Emitir warning se variável for usada sem atribuição anterior.
Pode ser simplificado para casos óbvios (sem analisar todos os caminhos).

🔴 Mais Difíceis
Inferência de tipo de expressões
→ A cada visit de operação (+, -, *, etc.), retornar tipo.
Implica estrutura de tipos e coerções (ex: int + double → double).

Verificação de tipos nos argumentos de função
→ Ex: f(int) ser chamado com f(double) → erro ou coerção.
Implica guardar assinatura de cada função.

Verificação de inicialização de vetores (blocoArray) com tipos válidos
→ Validar se os valores {1,2,3} são coerentes com o tipo do vetor.
Requer saber o tipo base do vetor.

Verificação de caminhos de execução com return em funções não-void
→ Ex: garantir que todos os ramos de uma função retornam algo.
Implica análise de fluxo de controlo.

Deteção de código inalcançável
→ Ex: instruções após return ou goto.
Exige rastreamento de fluxo de execução.

Análise de recursão (detetar ausência de caso base)
→ Muito dependente do objetivo — pode ser arbitrária.
Exige análise semântica profunda e heurísticas.
