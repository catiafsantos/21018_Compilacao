void main(void);
void main(void) {
	writes("Lê int ou double. insira int");
    int x=read(); /* Lê int ou double */
    write(x);
	
    writes("Lê int ou double. insira double");
    double y=read(); /* Lê int ou double */
	write(y);
	
	writes("Lê caracter (retorna valor ASCII)");
	int z=readc(); /* Lê caracter (retorna valor ASCII) */ 
	write(z);
	
	writes("Lê string para vetor de int (termina em 0)");
	int str[]=reads(); /* Lê string para vetor de int (termina em 0) */
	writev(str); 
	 
}
/*
9. Entrada/Saída
9.1. Entrada:
9.1.1. read(): Lê int ou double.
9.1.2. readc(): Lê caracter (retorna valor ASCII).
9.1.3. reads(): Lê string para vetor de int (termina em 0).

9.2. Saída:
9.2.1. write(x): Imprime valor de variável.
9.2.2. writec(x): Imprime caracter (ASCII).
9.2.3. writev(vetor): Imprime vetor no formato {1, 2, 0}.
9.2.4. writes("texto"): Imprime string (com \n ao final).
/*