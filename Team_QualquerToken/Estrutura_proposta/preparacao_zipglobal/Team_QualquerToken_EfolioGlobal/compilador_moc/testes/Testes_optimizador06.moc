/* teste de optimizacao de constant folding */
void main(void);
void main(void) {
	int a, b, c, d;
	a = 5;
	b = 10;
	c = a + (b * 2.0);
	d = c / 5.0;
	write(d);
	/* arrays */
	int e, f= read(), g=2*b, x[]={1,2,3};
	int y= x[0] + x[1];
	write(y);
}