void main(void);
void main(void) {
    int x;
    x = 65;
	vector[] = {65, 66, 67}
    write(x); /* imprime valor de variável */
    writes("abc"); /* imprime string com \n no final */
    writes("xpto"); /* imprime string com \n no final */
    writes("top"); /* imprime string com \n no final */
	writec(x); /* imprime caracter ASCII */
	writev(vector); /* Imprime vetor no formato {1, 2, 0} */
}
/*
9.2. Saída:
9.2.1. write(x): Imprime valor de variável.
9.2.2. writec(x): Imprime caracter (ASCII).
9.2.3. writev(vetor): Imprime vetor no formato {1, 2, 0}.
9.2.4. writes("texto"): Imprime string (com \n ao final).
/*